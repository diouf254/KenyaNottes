<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>KenyaNotes</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #e8f0fe; /* soft academic blue */
      color: #1a237e;
      text-align: center;
      margin: 0;
      padding: 0;
    }

    header {
      background-color: #1565c0;
      color: white;
      padding: 20px 0;
      box-shadow: 0 2px 6px rgba(0,0,0,0.2);
    }

    h1 {
      margin: 0;
      font-size: 2em;
      letter-spacing: 1px;
    }

    p.tagline {
      margin: 5px 0 0;
      font-size: 1.1em;
      font-weight: 400;
    }

    .buttons {
      margin: 40px 0;
    }

    .buttons a {
      text-decoration: none;
      background-color: #1565c0;
      color: white;
      padding: 12px 25px;
      border-radius: 8px;
      margin: 10px;
      display: inline-block;
      transition: background 0.3s;
    }

    .buttons a:hover {
      background-color: #0d47a1;
    }

    footer {
      margin-top: 40px;
      padding: 20px;
      font-size: 0.9em;
      background-color: #f1f1f1;
    }

    footer p {
      margin: 8px 0;
    }

    @media (max-width: 600px) {
      h1 {
        font-size: 1.6em;
      }
      .buttons a {
        display: block;
        width: 60%;
        margin: 15px auto;
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>KenyaNotes</h1>
    <p class="tagline">Your hub for university notes and past papers</p>
  </header>

  <section class="buttons">
    <a href="https://1drv.ms/f/c/02c8760e952247f7/EpHHz9C9RO9Ain5JE9R9f4ABKSebGMmu4j04j07XUSjVHQ" target="_blank">View Notes</a>
    <a href="https://forms.gle/yourformlink" target="_blank">Upload Notes</a>
  </section>

  <footer>
    <p>📞 Contact Admin: WhatsApp <strong>0769924553</strong></p>
    <p>📧 Email: <strong>dioufigonyi489@gmail.com</strong></p>
    <p><small>All uploads are verified by the admin before being shared.</small></p>
  </footer>

</body>
</html># This workflow will build and push a node.js application to an Azure Web App when a commit is pushed to your default branch.
#
# This workflow assumes you have already created the target Azure App Service web app.
# For instructions see https://docs.microsoft.com/en-us/azure/app-service/quickstart-nodejs?tabs=linux&pivots=development-environment-cli
#
# To configure this workflow:
#
# 1. Download the Publish Profile for your Azure Web App. You can download this file from the Overview page of your Web App in the Azure Portal.
#    For more information: https://docs.microsoft.com/en-us/azure/app-service/deploy-github-actions?tabs=applevel#generate-deployment-credentials
#
# 2. Create a secret in your repository named AZURE_WEBAPP_PUBLISH_PROFILE, paste the publish profile contents as the value of the secret.
#    For instructions on obtaining the publish profile see: https://docs.microsoft.com/azure/app-service/deploy-github-actions#configure-the-github-secret
#
# 3. Change the value for the AZURE_WEBAPP_NAME. Optionally, change the AZURE_WEBAPP_PACKAGE_PATH and NODE_VERSION environment variables below.
#
# For more information on GitHub Actions for Azure: https://github.com/Azure/Actions
# For more information on the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# For more samples to get started with GitHub Action workflows to deploy to Azure: https://github.com/Azure/actions-workflow-samples

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  AZURE_WEBAPP_NAME: your-app-name    # set this to your application's name
  AZURE_WEBAPP_PACKAGE_PATH: '.'      # set this to the path to your web app project, defaults to the repository root
  NODE_VERSION: '20.x'                # set this to the node version to use

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: npm install, build, and test
      run: |
        npm install
        npm run build --if-present
        npm run test --if-present

    - name: Upload artifact for deployment job
      uses: actions/upload-artifact@v4
      with:
        name: node-app
        path: .

  deploy:
    permissions:
      contents: none
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Development'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Download artifact from build job
      uses: actions/download-artifact@v4
      with:
        name: node-app

    - name: 'Deploy to Azure WebApp'
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: ${{ env.AZURE_WEBAPP_NAME }}
        publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
        package: ${{ env.AZURE_WEBAPP_PACKAGE_PATH }}
